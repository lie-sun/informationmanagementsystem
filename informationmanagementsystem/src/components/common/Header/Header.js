import React, {Component} from 'react';
import './Header.less'

class Header extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <header>
                header
            </header>
        );
    }
}

export default Header;